--gradle command
gradle build
gradle task --all (全部可执行的task)
gradle :web-app:listJars (查看依赖)
gradle :web-app:test --info --rerun-tasks --tests TestClass(单元测试)

--start kafka docker
docker run --name kfk -p2181:2181 -p9092:9092 -p2222:22 -v /tmp/:/jar -it kfk /entry -bash

--spring-boot-devtools SilentExitException
you can toggle the "suspend execution on uncaught exceptions" checkbox in Java -> Debug preferences to prevent it from happening.

--使用本地maven仓库
~/.m2/配置

--gradle user home指定本地仓库位置
export GRADLE_USER_HOME=$GRADLE_HOME

--avoid gradlew download 
sed -E  -i '' "s#distributionUrl=.*#distributionUrl=file\://${GRADLE_HOME}-bin.zip#" gradle/wrapper/gradle-wrapper.properties
